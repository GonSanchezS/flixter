  <br />
<div class="user-form">
  <h1 class="user-form--title">Add a new section</h1>
      <br />
   <%= simple_form_for @section, url: instructor_course_sections_path(@course) do |f| %>
    <%= f.input :title %>
        <br />
    <%= f.submit "Add a section", class: 'btn btn-primary user-form--button' %>
    <br />
  <% end %>
</div>